# Matt Pocock implementation order set

The user clarified that the Matt Pocock skills section should present the actual implementation order as `wayfinder`/`grill-with-docs` → `to-spec` → `to-tickets` → `implement` → `code review`. Future teaching artifacts should keep that order explicit and avoid presenting `implement` as the first step.
