---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Load both the `grilling` and `domain-modeling` skills from your runtime's skill directory (OMP: `read skill://grilling` and `read skill://domain-modeling`; Hermes: `skill_view` for each), and run them together: the interview from one, the glossary and ADR upkeep from the other.
